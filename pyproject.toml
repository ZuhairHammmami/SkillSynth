[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "skillsynth"
version = "1.0.0"

[project.scripts]
skillsynth = "backend.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[project.optional-dependencies]
ai = ["llama-cpp-python>=0.3"]
