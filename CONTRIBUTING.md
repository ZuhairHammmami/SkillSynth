# دليل المساهمة في المشروع

أهلاً بك في فريقنا! لمساعدتنا على البقاء منظمين، يرجى اتباع هذه الخطوات عند إضافة أي تعديل أو ميزة جديدة.

## خطوات العمل (Git Workflow)

1.  **إنشاء فرع جديد (Branch):**
    لا تعمل أبداً على الفرع الرئيسي (`main`). ابدأ دائماً بإنشاء فرع جديد خاص بالميزة التي تعمل عليها.
    ```bash
    # اختر اسماً وصفياً لفرعك (مثال: feature/add-login-button)
    git checkout -b <اسم-الفرع-الجديد>
    ```

2.  **كتابة الكود وحفظ التغييرات:**
    - قم بكتابة الكود الخاص بك.
    - استخدم هذه الأوامر لحفظ تقدمك:
    ```bash
    # أضف الملفات التي عدلتها
    git add .

    # احفظ التغييرات مع رسالة واضحة
    git commit -m "نوع-التغيير: وصف مختصر"
    ```

3.  **نمط رسائل الحفظ (Conventional Commits):**
    الرجاء استخدام البادئات التالية في رسائلك لتوضيح نوع التغيير:
    - `feat:` (لإضافة ميزة جديدة)
    - `fix:` (لإصلاح خطأ برمجي)
    - `docs:` (لتعديل التوثيق، مثل ملف README)
    - `style:` (لتعديلات التنسيق التي لا تؤثر على الكود)
    - `refactor:` (لإعادة هيكلة الكود بدون تغيير وظيفته)
    - `test:` (لإضافة أو تعديل الاختبارات)

    **مثال:** `git commit -m "feat: Add user registration endpoint"`

4.  **رفع الكود وفتح طلب دمج (Pull Request):**
    - ارفع فرعك إلى GitHub:
    ```bash
    git push -u origin <اسم-الفرع-الجديد>
    ```
    - اذهب إلى صفحة المشروع على GitHub، سيظهر لك زر لفتح **"Pull Request"** جديد. اضغط عليه، املأ النموذج، واطلب المراجعة.