// المسار: src/frontend/next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,
  // يمكنك إضافة أي إعدادات خاصة بـ Next.js هنا في المستقبل
};

module.exports = nextConfig;
