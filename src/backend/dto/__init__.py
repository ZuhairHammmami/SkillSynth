"""DTO package — four modules over the 15-table reduced schema.

auth.py (identity), catalog.py (catalog), learning.py (learning),
admin.py (admin + engagement feed). Consumers import directly from
submodules (e.g. `from backend.dto.auth import ProfileOut`).
"""
