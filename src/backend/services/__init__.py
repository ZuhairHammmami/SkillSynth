"""Service package — six business-logic modules over the reduced schema.

auth, catalog, learning, assess, analytics, admin. Import gate for
Task 2: every module must import cleanly against the new entities.
"""
