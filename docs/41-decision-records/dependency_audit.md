# Dependency Audit Report — SkillSynth

**Date:** 2026-06-23  
**Auditor:** Agent-0A (Dependency Inspector)

---

## 1. Frontend Package Audit (`src/frontend/package.json`)

### 1.1 Dependencies Found — 42 packages (30 production + 12 dev)

### 1.2 Confirmed In-Use Packages

| Package | Used In |
|---------|---------|
| `@radix-ui/*` (12 variants) | `src/shared/ui/*.tsx` — all 12 directly imported |
| `@supabase/ssr` | `src/lib/supabase.ts` — `createBrowserClient` |
| `@tanstack/react-query` | `useApi.ts`, `useMasteryData.ts`, `useLiveData.ts`, `providers.tsx`, `queryInvalidator.ts`, `usePrefetch.ts` — 90+ usages |
| `next-intl` | 17 page files + `i18n/provider.tsx` via `useTranslations` |
| `axios` | `src/shared/lib/api.ts` |
| `class-variance-authority` | `button.tsx`, `badge.tsx`, `alert.tsx` via `cva()` |
| `clsx` + `tailwind-merge` | `utils.ts` (`cn()`), used in 93+ locations across all UI components |
| `js-cookie` | `useWebSocket.ts`, `useSSE.ts`, `useApi.ts`, `api.ts` |
| `next` | Core framework |
| `react` / `react-dom` | Core |
| `react-hook-form` | `form.tsx` — `useFormContext`, `Controller`, `useFormState` |
| `sonner` | `sonner.tsx`, `useToast.ts`, `ConflictNotificationService.ts` |
| `uuid` | `AssessmentService.ts`, `StuckProtocolService.ts` |
| `zod` | `entities/UserPath.ts`, `KnowledgeNode.ts`, `Assessment/index.ts` |
| `zustand` | `authStore.ts` |
| `lucide-react` | (assumed — heavily used in UI, confirm via build) |

### 1.3 UNUSED Packages — Candidates for Removal

| Package | Evidence |
|---------|----------|
| **`framer-motion`** `^12.23.26` | **Zero imports** in source code. Not present in `pnpm-lock.yaml` either. Only the stale root `pnpm-lock.yaml` references it. **Completely vestigial.** |
| **`@tanstack/react-table`** `^8.21.3` | **Zero imports** in source code. No `useReactTable`, no `getCoreRowModel`. |
| **`@hookform/resolvers`** `^5.2.2` | **Zero imports** in source code. No `zodResolver` or similar. |
| **`react-player`** `^3.4.0` | Only a `.d.ts` type declaration (`declare module 'react-player/lazy'`). No component usage. |
| **`next-themes`** `^0.2.1` | **Zero imports** in source code. No `ThemeProvider` or `useTheme`. |

**Savings from removing these 5 packages:** ~250KB+ gzipped (framer-motion alone is ~150KB gzipped).

### 1.4 DevDependencies Assessment

| Package | Status |
|---------|--------|
| `@types/js-cookie` | Used (type for `js-cookie`) ✓ |
| `@tanstack/react-query-devtools` | Lazy-imported in `providers.tsx:9` ✓ |
| `@types/node`, `@types/react`, `@types/react-dom`, `@types/uuid` | Required for TypeScript ✓ |
| `autoprefixer`, `postcss`, `tailwindcss`, `tailwindcss-animate` | Required for Tailwind ✓ |
| `eslint`, `eslint-config-next` | Linting ✓ |
| `typescript` | Type-checking ✓ |

No unused devDependencies found.

---

## 2. Lock File Conflicts

### 2.1 File Inventory

| Lock File | Size | Last Modified | Status |
|-----------|------|---------------|--------|
| `src/frontend/pnpm-lock.yaml` | 207 KB / 5817 lines | Jun 16 04:12 | **Source of truth** ✅ |
| `src/frontend/package-lock.json` | 306 KB / 8341 lines | Jun 13 17:26 | **Stale — conflict** ❌ |
| `ROOT/pnpm-lock.yaml` | 3.7 KB / 125 lines | Apr 20 23:32 | **Stale — wrong location** ❌ |

### 2.2 Conflict Analysis

**`src/frontend/package-lock.json`** — This file should NOT exist when using pnpm. It was likely generated by an accidental `npm install`. It is stale (Jun 13) compared to `pnpm-lock.yaml` (Jun 16). It must be deleted.

**`ROOT/pnpm-lock.yaml`** — This file references completely different versions:
- React **19.2.5** (package.json specifies `^18`)
- `framer-motion` **12.38.0** (package.json `^12.23.26` — but unused anyway)
- `lucide-react` **1.8.0** (package.json `^0.379.0`)
- `uuid` **14.0.0** (package.json `^9.0.1`)
- `@types/uuid` **11.0.0** (package.json `^9.0.7`)
- `next-themes` **0.4.6** (package.json `^0.2.1`)

This lock file is completely incompatible and was likely created by running `pnpm install` from the project root instead of `src/frontend/`. It must be deleted.

### 2.3 pnpm-workspace.yaml

File at `src/frontend/pnpm-workspace.yaml`:
```yaml
allowBuilds:
  unrs-resolver: true
```

This is a minimal config that only whitelists `unrs-resolver` builds. It does **not** define any workspace packages (no `packages:` array). The project is effectively **not using pnpm workspaces** despite the root `pnpm-lock.yaml` implying it might.

---

## 3. Python Dependency Audit

### 3.1 `requirements.txt` (15 packages listed)

| Package | Listed | Installed | Used in Backend | Status |
|---------|--------|-----------|-----------------|--------|
| `fastapi` | ✅ | 0.136.3 | Yes — framework | ✓ |
| `uvicorn[standard]` | ✅ | 0.49.0 | Yes — ASGI server | ✓ |
| `python-multipart` | ✅ | 0.0.32 | Yes — form parsing | ✓ |
| `SQLAlchemy>=1.4` | ✅ | 2.0.50 | Yes — ORM | ✓ |
| `psycopg2-binary` | ✅ | 2.9.12 | Yes — Postgres driver | ✓ |
| `pydantic>=2.0` | ✅ | 2.13.4 | Yes — schemas | ✓ |
| `python-dotenv` | ✅ | 1.2.2 | Yes — env config | ✓ |
| `email-validator` | ✅ | 2.3.0 | Yes — validation | ✓ |
| `passlib==1.7.4` | ✅ | 1.7.4 | Yes — password hashing | ✓ |
| `bcrypt==3.2.0` | ✅ | 3.2.0 | Yes — passlib backend | ✓ |
| `python-jose[cryptography]` | ✅ | 3.5.0 | Yes — JWT tokens | ✓ |
| `slowapi` | ✅ | 0.1.10 | Yes — rate limiting | ✓ |
| `redis>=4.0` | ✅ | ❌ **NOT INSTALLED** | Yes — `cache_service.py` lazy import | ❌ **BROKEN** |
| `openai>=1.0.0` | ✅ | 2.41.1 | **No imports found** | ⚠️ Unused (future?) |
| `langchain>=0.1.0` | ✅ | 1.3.9 | **No imports found** | ⚠️ Unused (future?) |
| `langchain-openai>=0.0.1` | ✅ | 1.3.2 | **No imports found** | ⚠️ Unused (future?) |
| `langchain-community>=0.0.1` | ✅ | 0.4.2 | **No imports found** | ⚠️ Unused (future?) |
| `ollama>=0.1.0` | ✅ | 0.6.2 | **No imports found** | ⚠️ Unused (future?) |

### 3.2 MISSING from `requirements.txt` (but installed + used)

| Package | Version | Used In | Urgency |
|---------|---------|---------|---------|
| **`sendgrid`** | 6.12.5 | `src/backend/services/email_service.py:3` — **CRITICAL** | 🔴 Must add to requirements.txt |

### 3.3 Installed but NOT in `requirements.txt` (likely transitive)

| Package | Likely Origin | Action |
|---------|---------------|--------|
| `httpx` | langchain dependency | No action (transitive) |
| `httpx-sse` | langchain dependency | No action |
| `langgraph*` | langchain ecosystem | No action (transitive) |
| `werkzeug` | passlib/indirect dep | No action |
| `pillow` | transitive | No action |
| `numpy` | transitive | No action |
| `tenacity` | langchain/retry dep | No action |
| `tiktoken` | openai tokenizer dep | No action |
| `jsonpatch`/`jsonpointer` | transitive | No action |
| `playwright` | E2E testing tool | ⚠️ Add to dev-requirements or remove |
| `pyee` | playwright dependency | No action |

### 3.4 CRITICAL ISSUE: `redis` NOT Installed

`redis>=4.0` is in `requirements.txt` but `pip list` shows **no redis package installed**. The backend has a lazy import of `redis` in `cache_service.py:25`. If the caching path is triggered, it will raise `ModuleNotFoundError`.

---

## 4. ESM/CJS Compatibility Assessment

### 4.1 Frontend (`src/frontend/`)

| File | Module System | Assessment |
|------|---------------|------------|
| `next.config.js` | `module.exports` (CJS) | ✅ Correct — Next.js requires CJS for config |
| `tsconfig.json` | `"module": "esnext"`, `"moduleResolution": "bundler"` | ✅ Correct for Next.js 14 SWC bundler |
| All source files | ESM (`import`/`export`) | ✅ Next.js handles transpilation |

**Verdict:** No ESM/CJS issues in frontend. Next.js 14 with `"moduleResolution": "bundler"` handles everything.

### 4.2 CLI (`tools/cli/`)

| File | Module System | Assessment |
|------|---------------|------------|
| `tsconfig.json` | `"module": "commonjs"` | ✅ CJS output — correct for Node.js CLI |
| `chalk@^4.1.2` | CJS only | ✅ Compatible with CJS output |
| `ora@^5.4.1` | CJS only | ✅ Compatible with CJS output |
| `conf@^12.0.0` | **ESM only** | ❌ **POTENTIAL ISSUE** — v12 of `conf` is ESM-only. When compiled to CJS by tsc, `require()` will fail. The CLI may need `esModuleInterop` or dynamic `import()`. |

**Action:** Test `conf@^12.0.0` import at runtime. Downgrade to `conf@^11.0.0` (CJS) or switch CLI to ESM output.

---

## 5. Cross-Project Duplicate Packages

| Package | Frontend (`package.json`) | CLI (`package.json`) | Conflict? |
|---------|---------------------------|----------------------|-----------|
| `axios` | `^1.7.2` | `^1.6.5` | Minor version diff, no conflict |
| `zod` | `^4.2.1` | `^4.2.1` | ✅ Same version |
| `typescript` | `^5` | `^5.3.3` | ✅ Compatible |
| `@types/node` | `^20` | `^20.10.6` | ✅ Compatible |

No blocking duplications. Versions are compatible across projects.

---

## 6. Summary of Issues Found

| # | Severity | Issue | File | Action |
|---|----------|-------|------|--------|
| 1 | 🔴 | `redis` not installed | `requirements.txt` | Run `pip install redis` |
| 2 | 🔴 | `sendgrid` missing from requirements | `requirements.txt` | Add `sendgrid>=6.0` |
| 3 | 🟠 | `framer-motion` completely unused | `package.json` | Remove dep + delete stale root lock |
| 4 | 🟠 | `@tanstack/react-table` unused | `package.json` | Remove dep |
| 5 | 🟠 | `@hookform/resolvers` unused | `package.json` | Remove dep |
| 6 | 🟠 | `react-player` unused | `package.json` | Remove dep, remove `.d.ts` stub |
| 7 | 🟠 | `next-themes` unused | `package.json` | Remove dep |
| 8 | 🟠 | `package-lock.json` conflicts with `pnpm-lock.yaml` | `src/frontend/` | Delete `package-lock.json` |
| 9 | 🟠 | Stale `pnpm-lock.yaml` at project root | `ROOT/pnpm-lock.yaml` | Delete file |
| 10 | 🟡 | `conf@^12.0.0` ESM-only in CJS CLI | `tools/cli/` | Downgrade to `conf@^11` or switch to ESM |
| 11 | 🟡 | LLM deps (`openai`, `langchain*`, `ollama`) unused | `src/backend/*.py` | Consider if needed; comment or remove |
| 12 | ⚪ | `playwright` installed but not in requirements | System | Add to dev-requirements or remove |

---

## 7. Recommended Cleanup Commands

### 7.1 Lock File Cleanup
```bash
# Remove conflicting/stale lock files
rm /media/zuhair/Extra/SkillSynth/src/frontend/package-lock.json
rm /media/zuhair/Extra/SkillSynth/pnpm-lock.yaml
```

### 7.2 Frontend Unused Packages
```bash
cd /media/zuhair/Extra/SkillSynth/src/frontend
pnpm remove framer-motion @tanstack/react-table @hookform/resolvers react-player next-themes
```

Also remove the type declaration in `src/types.d.ts`:
```
declare module 'react-player/lazy';   # DELETE THIS LINE
```

### 7.3 Python Dependency Fixes
```bash
source /media/zuhair/Extra/SkillSynth/.venv/bin/activate

# Install redis (missing)
pip install "redis>=4.0"

# Add sendgrid to requirements
pip install "sendgrid>=6.0"

# Update requirements.txt
pip freeze | grep -E "^(redis|sendgrid)" >> /media/zuhair/Extra/SkillSynth/requirements.txt
```

### 7.4 CLI Compatibility Fix
Option A — Downgrade `conf` to CJS version:
```bash
cd /media/zuhair/Extra/SkillSynth/tools/cli
npm install conf@^11.0.0
```

Option B — Switch CLI to ESM (add `"type": "module"` to `package.json` and change `tsconfig.json` `"module": "NodeNext"`), then update `chalk` and `ora` to v6+:
```bash
cd /media/zuhair/Extra/SkillSynth/tools/cli
npm install chalk@^5.3.0 ora@^8.0.0 conf@^12.0.0
```

### 7.5 Redis Verification
After installing `redis`, test the import:
```bash
source /media/zuhair/Extra/SkillSynth/.venv/bin/activate
python -c "import redis; print('OK:', redis.__version__)"
```

---

## 8. Dependency Count Summary

| Domain | Listed | Installed | Unused | Missing |
|--------|--------|-----------|--------|---------|
| Frontend (npm) | 42 | ~42 | 5 | 0 |
| CLI (npm) | 12 | ~12 | 0 | 0 |
| Backend (pip) | 18 (in reqs) | ~84 (with transitive) | ~5 (LLM deps) | 2 (`redis` missing, `sendgrid` unlisted) |

---

*Report generated by Agent-0A (Dependency Inspector).*
